# Fixture Node Add-on Release Note

Valid node-addon fixture package for search and install tests.

It installs `fixture/release-note@1`, a declarative worker node template that
drafts a short release note from `addon.inputs.topic`.
