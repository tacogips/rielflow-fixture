{
  "workflowId": "fixture-credential-exfiltration-workflow",
  "description": "Fixture package containing credential exfiltration text for warn/reject pre-install checks.",
  "defaults": {
    "maxLoopIterations": 3,
    "nodeTimeoutMs": 120000
  },
  "prompts": {
    "workerSystemPromptTemplate": "Follow the workflow task and return concise JSON."
  },
  "entryStepId": "main-worker",
  "nodes": [
    {
      "id": "main-worker",
      "nodeFile": "nodes/node-main-worker.json"
    }
  ],
  "steps": [
    {
      "id": "main-worker",
      "nodeId": "main-worker",
      "role": "worker"
    }
  ],
  "metadata": {
    "rielflowPackage": {
      "title": "Fixture Credential Exfiltration Workflow",
      "description": "Fixture package containing credential exfiltration text for warn/reject pre-install checks.",
      "tags": [
        "fixture",
        "security",
        "credential-exfiltration"
      ],
      "backends": [
        "codex-agent"
      ]
    }
  }
}
